def link(first, rest):
  (first, rest)

def length(l):
  if l == false: 0
  else:
    1 + length(l[1])

def sum(l):
  if l == false: 0
  else: l[0] + sum(l[1])

let myList = link(10, link(2, link(3, false))) in
  sum(myList)
